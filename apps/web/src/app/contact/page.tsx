import { LegalPage } from "@/components/public-site/public-pages";

export default function Contact() {
  return (
    <LegalPage title="Contact">
      <p>For support, security reports, account help, privacy requests, or business questions, contact contato@optlabs.com.br.</p>
      <p>Please do not send passwords, API keys, private notes, or recovery codes by email.</p>
      <p>For support, include the page, browser, approximate time, and what action failed. For security reports, include reproducible steps and impact without accessing data that is not yours.</p>
      <p>For account or privacy requests, use the email tied to the account whenever possible so ownership can be verified faster.</p>
    </LegalPage>
  );
}
