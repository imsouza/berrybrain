# BerryBrain Clean Baseline - 2026-07-13

## Environment

- Disposable Docker Compose project: `berrybrain-baseline`
- Empty named data volume and empty temporary vault
- API: `http://127.0.0.1:18000`
- Web: `http://127.0.0.1:13000/berrybrain`
- Local Ollama endpoint intentionally unavailable
- Command: `scripts/baseline.sh`

## Results

| Measurement | Result |
|---|---:|
| Time until API, Web, and Worker were healthy | approximately 65 s |
| Note creation | 6 s |
| Complete note pipeline | 163 s |
| Pipeline completion | 100% |
| Semantic search with local provider unavailable | 2.023064 s |
| Dead-letter jobs | 0 |

## Observations

- The Worker processed all 14 pipeline stages in dependency order.
- Provider-dependent stages degraded explicitly to deterministic processing.
- Search failed fast on the unavailable local embedding provider and used the lexical/graph fallback instead of waiting for the 30-second request timeout.
- Worker heartbeat timestamps were serialized as UTC and the Compose healthcheck remained healthy.
- The run used a disposable vault and database; it did not touch the user's live stack or vault.

## Reproduction

```bash
BERRYBRAIN_API_PORT=18000 \
BERRYBRAIN_WEB_PORT=13000 \
BERRYBRAIN_HOST_VAULT_PATH=/tmp/berrybrain-baseline-vault \
docker compose -p berrybrain-baseline up -d --no-build

BERRYBRAIN_BASELINE_API_URL=http://127.0.0.1:18000 \
BERRYBRAIN_BASELINE_WEB_URL=http://127.0.0.1:13000/berrybrain \
sh scripts/baseline.sh
```
